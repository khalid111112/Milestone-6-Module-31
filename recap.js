/* 
1. var let const
2.default parameter
3. temolate string 
4. arrow function
5.destructuring and spread operting
6. bject. keys, object.values, object.entries
7. for of used in array and during,
8. for un loop used un object 

*/

const a = 56;
const numbers = [56, 7, 8]
const person = {
name: 'sakib khan'
}
const mssage = 'Hi, ${person.name} has a: ${}a access to ${numbers}'

if(true){

}

const square = x => x * x;
const sum = (a, b) => a + b;


const {} = {x: 2, y: 5, z: 3, name: 'ovijeet',age: 55}
const [first, second, ...renaining] = ['ram', 'sam', 'jodu', 'modhu']